services:
  spotify-analytics:
    build: .
    container_name: spotify-analytics
    restart: unless-stopped
    ports:
      - "3015:3000"
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"

